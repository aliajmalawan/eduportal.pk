/* Superseded by js/reveal.js, which handles .reveal, .reveal-fd and
 * .reveal-cs with one observer, one threshold and one easing.
 * Kept as an empty module so any page still referencing it 404s nothing. */
